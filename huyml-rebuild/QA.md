# 验证记录

日期：2026-09-15。对照源：公开的 https://huyml.co/ 。

## 自动检查

`npm run validate` 全部通过：

- TypeScript 严格类型检查。
- 19 个唯一项目路由、83 个唯一实验作品、9 篇实际兴趣故事。
- 195 处图片内容、12 个实验视频入口，另有全站 Showreel。
- 项目后继链接全部有效；最后一个项目循环回第一个项目。
- 内容图片、封面、字体、Rive/WASM 等静态源码引用及视频封面存在且非空。
- 生产构建成功，应用、React、Three、GSAP、Rive 和 About 分包；最大 JS 包小于 500 kB。

Vite 对 React Router 的 `use client` 指令给出忽略提示，不影响构建或客户端运行。

## 浏览器实际操作

桌面 1280×720 与手机 390×844 对照验证：

- 首页 WebGL 卡片可见；滚动更新项目；点击卡片跳入 Iventions 项目详情。
- About 人物 Rive 正常加载；点击头部展开，点击猫咪图标打开故事；故事内容与原站实际弹层相符。
- Process & Approach、Contact/Credits 能打开，Escape 能关闭。
- Playground 首个作品打开预览；Next 切换至第二个作品并更新计数。
- 手机菜单展开并跳转 About，菜单在跳转后关闭。
- 手机项目标题、正文、画廊可见；首张图片能打开放大预览。
- 声音开关从 Off 切换 On 并可复原；复制邮箱出现 Copied 状态。
- 生产服务器首页 WebGL 正常渲染。
- 手机首页和 About 的 scrollWidth 等于视口宽度 390，无横向溢出。

完整内容通过数据与资源审计；浏览器操作覆盖共用页面模板和上述交互，没有把每一个项目、每一个媒体项或每一种设备逐个手动穷举。

## 外部依赖与边界

Vimeo 的公开 oEmbed 信息与 13 张视频封面成功取得。当前浏览器中的播放器未成功显示视频，配置接口返回 HTTP 403；无法在此环境确认实际视频播放。保留公开 iframe，增加本地封面和 Watch on Vimeo 链接。未绕过视频权限，也未提取受限视频流。

图片、字体、音效、Rive 资源及 WASM 存放本地。Rive 插画与原站 gallery shader 为原始视觉资源，React 页面与交互为独立实现。转场、惯性及弹层运动不是逐帧复刻。SVG、GLSL、CSS、TSX、JSON 可分别编辑；人物原始形状/动画通过 Rive 文件编辑。

截图保存在 `artifacts/`。
